import React from 'react';
import { InsertNewComment } from './InsertNewComment'

export default {
    title: "components/InsertNewComment",
    component: InsertNewComment,
}

const Template = () => <InsertNewComment/>

export const InsertNewCommentTemplate = Template.bind({});